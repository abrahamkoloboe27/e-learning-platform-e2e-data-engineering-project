# MongoDB Atlas connection string (never commit real credentials)
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster-url>/?retryWrites=true&w=majority&appName=<app-name>
MONGODB_DB_NAME=elearning_synthetic

# Optional defaults (can be overridden by CLI)
BATCH_SIZE=1000
DEFAULT_SEED=42
